import { C as shouldInjectSubpackageStyleImport, E as createStyleInjector, O as mergePerFileResolvers, S as resolveSubpackageStyleImport, T as PLUGIN_NAME, _ as isSubpackageStyleOutputFile, c as collectSubpackageStyleAssets, g as isSourceModuleTargetFile, l as collectSubpackageTargetStyleAssets, m as isMatchedSourceModuleTargetFile, t as createUniAppSubPackageImportResolver } from "./uni-app-Dz-QaV_N.js";
//#region src/vite.ts
function weappStyleInjector(options = {}) {
	const { uniAppSubPackages, uniAppStyleScopes, perFileImports, subpackageStyleScopes, generateSubpackageStyle, loadSubpackageTargetStyle, ...restOptions } = options;
	const perFileResolver = mergePerFileResolvers([typeof perFileImports === "function" ? perFileImports : void 0, subpackageStyleScopes && subpackageStyleScopes.length > 0 ? void 0 : createUniAppSubPackageImportResolver(uniAppSubPackages, uniAppStyleScopes)]);
	const injectorOptions = { ...restOptions };
	if (perFileResolver !== void 0) injectorOptions.perFileImports = perFileResolver;
	const injector = createStyleInjector(injectorOptions);
	return {
		name: PLUGIN_NAME,
		apply: "build",
		enforce: "post",
		generateBundle: {
			order: "post",
			async handler(_, bundle) {
				const readAssets = () => Object.entries(bundle).filter(([, output]) => output.type === "asset").map(([fileName, output]) => {
					const asset = { fileName };
					if (output.type === "asset" && typeof output.source !== "undefined") asset.source = output.source;
					return asset;
				});
				const targetAssets = collectSubpackageTargetStyleAssets(subpackageStyleScopes ?? [], readAssets());
				for (const asset of targetAssets) {
					if (bundle[asset.fileName]) continue;
					const source = asset.sourceAbsolutePath && loadSubpackageTargetStyle ? await loadSubpackageTargetStyle(asset.fileName, asset.sourceAbsolutePath) : void 0;
					bundle[asset.fileName] = {
						type: "asset",
						fileName: asset.fileName,
						name: asset.fileName,
						names: [asset.fileName],
						originalFileName: null,
						originalFileNames: [],
						needsCodeReference: false,
						source: source ?? ""
					};
				}
				const styleAssets = readAssets().filter((asset) => injector.shouldProcess(asset.fileName));
				const subpackageAssets = collectSubpackageStyleAssets(subpackageStyleScopes ?? [], styleAssets);
				for (const asset of subpackageAssets) {
					const generator = asset.scope.generate ?? generateSubpackageStyle;
					if (!generator) continue;
					const generated = await generator({
						root: asset.scope.root,
						sourcePath: asset.scope.sourceAbsolutePath,
						sourceFiles: asset.scope.sourceFiles ?? [asset.scope.sourceAbsolutePath],
						pageStyleFiles: asset.pageStyleFiles,
						outputFileName: asset.outputFileName,
						styleExt: asset.styleExt,
						framework: asset.scope.framework,
						bundler: "vite"
					});
					if (generated == null) continue;
					const existing = bundle[asset.outputFileName];
					if (existing?.type === "asset") existing.source = generated;
					else this.emitFile({
						type: "asset",
						fileName: asset.outputFileName,
						source: generated
					});
				}
				if (!injector.hasImports && (!subpackageStyleScopes || subpackageStyleScopes.length === 0)) return;
				for (const [fileName, output] of Object.entries(bundle)) {
					if (output.type !== "asset") continue;
					if (!injector.shouldProcess(fileName)) continue;
					if (subpackageStyleScopes?.some((scope) => isSubpackageStyleOutputFile(fileName, scope, subpackageStyleScopes))) continue;
					const source = typeof output.source === "undefined" ? "" : output.source;
					const subpackageImportPaths = subpackageStyleScopes ? subpackageStyleScopes.flatMap((scope) => {
						if (isSubpackageStyleOutputFile(fileName, scope, subpackageStyleScopes)) return [];
						if (!scope.sourceInclude && !scope.sourceExclude && isSourceModuleTargetFile(scope, fileName)) return [];
						if ((scope.sourceInclude || scope.sourceExclude) && !isMatchedSourceModuleTargetFile(scope, fileName)) return [];
						if (!shouldInjectSubpackageStyleImport(fileName, source, scope)) return [];
						const resolved = resolveSubpackageStyleImport(fileName, scope);
						return resolved ? [resolved] : [];
					}) : [];
					const result = subpackageImportPaths.length > 0 ? createStyleInjector({
						...injectorOptions,
						imports: subpackageImportPaths
					}).inject(fileName, source) : injector.inject(fileName, source);
					if (!result.changed) continue;
					output.source = result.content;
				}
			}
		}
	};
}
//#endregion
export { weappStyleInjector as default, weappStyleInjector };
