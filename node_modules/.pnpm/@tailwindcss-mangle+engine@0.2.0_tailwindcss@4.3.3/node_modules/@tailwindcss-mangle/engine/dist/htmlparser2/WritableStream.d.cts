import { r as ParserOptions, t as Handler } from "../Parser-DxhIcQSO.cjs";
import { Writable } from "node:stream";
//#region ../../node_modules/.pnpm/htmlparser2@12.0.0/node_modules/htmlparser2/dist/WritableStream.d.ts
/**
 * WritableStream makes the `Parser` interface available as a NodeJS stream.
 * @see Parser
 */
declare class WritableStream extends Writable {
  private readonly _parser;
  private readonly _decoder;
  constructor(cbs: Partial<Handler>, options?: ParserOptions);
  _write(chunk: string | Buffer, encoding: string, callback: () => void): void;
  _final(callback: () => void): void;
}
//#endregion
export { WritableStream };