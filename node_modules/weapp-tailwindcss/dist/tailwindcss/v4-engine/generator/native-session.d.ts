import type { GenerationRequest, SourceEntry, TailwindGenerationArtifact, TailwindGenerationSession, TailwindV4CandidateSource, TailwindV4ResolvedSource } from '@tailwindcss-mangle/engine';
import type { TailwindV4GenerateTarget } from '../types.js';
type CreateTailwindGenerationSession = (source: TailwindV4ResolvedSource) => TailwindGenerationSession;
export declare class TailwindV4NativeSessionPool {
    private readonly createSession;
    private readonly sessions;
    private disposed;
    constructor(createSession?: CreateTailwindGenerationSession);
    generate(target: TailwindV4GenerateTarget, sourceKey: string, source: TailwindV4ResolvedSource, request: GenerationRequest): Promise<TailwindGenerationArtifact>;
    dispose(): void;
    get size(): number;
    private getSession;
    private disposeEntry;
}
export declare function createEngineSourceEntries(sources: TailwindV4CandidateSource[], sourceId: string): SourceEntry[];
export declare function serializeTailwindGenerationArtifact(artifact: TailwindGenerationArtifact): string;
export {};
