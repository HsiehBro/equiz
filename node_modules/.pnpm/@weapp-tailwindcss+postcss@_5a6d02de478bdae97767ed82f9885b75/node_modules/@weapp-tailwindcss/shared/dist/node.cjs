Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const require_rolldown_runtime = require("./rolldown-runtime-VH7oDXx4.cjs");
let node_crypto = require("node:crypto");
node_crypto = require_rolldown_runtime.__toESM(node_crypto, 1);
let pathe = require("pathe");
pathe = require_rolldown_runtime.__toESM(pathe, 1);
//#region src/node.ts
function md5(input) {
	return node_crypto.default.createHash("md5").update(input).digest("hex");
}
function removeAllExtensions(filePath) {
	if (!filePath) return "";
	let baseName = pathe.default.basename(filePath);
	let ext = pathe.default.extname(baseName);
	while (ext) {
		baseName = baseName.slice(0, -ext.length);
		ext = pathe.default.extname(baseName);
	}
	const dir = pathe.default.dirname(filePath);
	return dir === "." ? baseName : pathe.default.join(dir, baseName);
}
//#endregion
exports.md5 = md5;
exports.removeAllExtensions = removeAllExtensions;
