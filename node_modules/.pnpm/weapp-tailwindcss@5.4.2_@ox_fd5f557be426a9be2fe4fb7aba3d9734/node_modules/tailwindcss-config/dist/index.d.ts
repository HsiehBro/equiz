import type { Config } from 'tailwindcss';
export interface LoadConfigOptions {
    cwd: string;
    config: string;
    moduleName: string;
}
export type LoadConfigResult = null | {
    filepath: string;
    config: Config;
    isEmpty?: boolean;
};
export declare function loadConfig(options?: Partial<LoadConfigOptions>): Promise<LoadConfigResult>;
