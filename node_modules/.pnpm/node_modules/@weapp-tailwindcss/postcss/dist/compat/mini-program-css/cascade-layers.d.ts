import type { Root } from 'postcss';
/**
 * 按 cascade layer 声明顺序重排规则并移除 `@layer` 语法。
 *
 * 该转换只模拟 layer 的顺序语义，不通过提高选择器权重模拟完整 specificity 规则。
 */
export declare function consumeCascadeLayers(root: Root): void;
