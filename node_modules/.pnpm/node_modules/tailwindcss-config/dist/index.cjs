Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
//#region \0rolldown/runtime.js
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __copyProps = (to, from, except, desc) => {
	if (from && typeof from === "object" || typeof from === "function") for (var keys = __getOwnPropNames(from), i = 0, n = keys.length, key; i < n; i++) {
		key = keys[i];
		if (!__hasOwnProp.call(to, key) && key !== except) __defProp(to, key, {
			get: ((k) => from[k]).bind(null, key),
			enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable
		});
	}
	return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(isNodeMode || !mod || !mod.__esModule || !__hasOwnProp.call(mod, "default") ? __defProp(target, "default", {
	value: mod,
	enumerable: true
}) : target, mod));
//#endregion
let node_path = require("node:path");
node_path = __toESM(node_path, 1);
let node_process = require("node:process");
node_process = __toESM(node_process, 1);
let jiti = require("jiti");
let lilconfig = require("lilconfig");
let _weapp_tailwindcss_shared = require("@weapp-tailwindcss/shared");
//#region src/index.ts
const jiti$1 = (0, jiti.createJiti)(require("url").pathToFileURL(__filename).href);
async function loadConfig(options) {
	const { config, cwd, moduleName } = (0, _weapp_tailwindcss_shared.defuOverrideArray)(options, {
		cwd: node_process.default.cwd(),
		moduleName: "tailwind"
	});
	const searcher = (0, lilconfig.lilconfig)("tailwindcss", {
		searchPlaces: [
			`${moduleName}.config.js`,
			`${moduleName}.config.cjs`,
			`${moduleName}.config.mjs`,
			`${moduleName}.config.ts`,
			`${moduleName}.config.cts`,
			`${moduleName}.config.mts`
		],
		loaders: {
			".js": jiti$1,
			".cjs": jiti$1,
			".mjs": jiti$1,
			".ts": jiti$1,
			".cts": jiti$1,
			".mts": jiti$1
		}
	});
	if (typeof config === "string" && config) {
		const configPath = node_path.default.isAbsolute(config) ? config : node_path.default.resolve(cwd, config);
		return await searcher.load(configPath);
	} else return await searcher.search(cwd);
}
//#endregion
exports.loadConfig = loadConfig;
