const require_uni_app = require("./uni-app-ea5GDqGI.cjs");
const require_preset_resolution = require("./preset-resolution-DcjjQ3Cf.cjs");
const require_webpack = require("./webpack.cjs");
let node_fs = require("node:fs");
node_fs = require_uni_app.__toESM(node_fs, 1);
let node_path = require("node:path");
node_path = require_uni_app.__toESM(node_path, 1);
let node_process = require("node:process");
node_process = require_uni_app.__toESM(node_process, 1);
let _weapp_tailwindcss_shared = require("@weapp-tailwindcss/shared");
//#region src/mpx.ts
const DEFAULT_STYLE_FILENAMES = [
	"index.css",
	"index.wxss",
	"index.scss",
	"index.less",
	"index.sass",
	"index.styl"
];
const SCRIPT_RE = /<script([^>]*)>([\s\S]*?)<\/script>/gi;
const JSON_SCRIPT_TYPE_RE = /\btype=["']application\/json["']/i;
function extractJsonScript(source) {
	SCRIPT_RE.lastIndex = 0;
	for (let match = SCRIPT_RE.exec(source); match; match = SCRIPT_RE.exec(source)) if (JSON_SCRIPT_TYPE_RE.test(match[1] ?? "")) return match[2];
}
function loadAppConfig(filePath) {
	const resolvedPath = node_path.default.resolve(filePath);
	if (!node_fs.default.existsSync(resolvedPath) || !node_fs.default.statSync(resolvedPath).isFile()) return null;
	try {
		const raw = node_fs.default.readFileSync(resolvedPath, "utf8");
		const jsonSource = resolvedPath.endsWith(".mpx") ? extractJsonScript(raw) : raw;
		if (!jsonSource) return null;
		return JSON.parse(jsonSource);
	} catch {
		return null;
	}
}
function resolveMpxSubPackages(config) {
	const appPath = node_path.default.resolve(config.appPath);
	const appConfig = loadAppConfig(appPath);
	if (!appConfig) return [];
	const primary = (0, _weapp_tailwindcss_shared.toArray)(appConfig["subPackages"]);
	const secondary = (0, _weapp_tailwindcss_shared.toArray)(appConfig["subpackages"]);
	const subPackagesInput = [...primary, ...secondary];
	if (subPackagesInput.length === 0) return [];
	const sourceRoot = node_path.default.resolve(config.sourceRoot ?? node_path.default.dirname(appPath));
	const entries = require_uni_app.normalizeSubpackageStyleRules(config.rules);
	const styleRules = entries.length > 0 ? entries : [require_uni_app.createDefaultStyleEntry(config, (0, _weapp_tailwindcss_shared.toArray)(config.sourceFileName))];
	return require_uni_app.resolveSubpackageStyleScopes({
		framework: "mpx",
		sourceRoot,
		subPackages: subPackagesInput,
		styleEntries: styleRules,
		defaultStyleCandidates: DEFAULT_STYLE_FILENAMES,
		preprocess: config.preprocess,
		collectTargets: require_uni_app.collectPageTargets
	});
}
function resolveDefaultMpxAppPaths() {
	const cwd = node_process.default.cwd();
	return [
		node_path.default.resolve(cwd, "src/app.mpx"),
		node_path.default.resolve(cwd, "app.mpx"),
		node_path.default.resolve(cwd, "src/app.json"),
		node_path.default.resolve(cwd, "app.json")
	];
}
//#endregion
//#region src/webpack/mpx.ts
function StyleInjector(options = {}) {
	const { appPath, sourceRoot, subPackages, sourceFileName, outputName, files, include, exclude, rules, ...rest } = options;
	const resolvedSubPackages = require_preset_resolution.collectPresetConfigs({
		explicitConfigs: subPackages,
		requestedPaths: appPath,
		defaultPaths: resolveDefaultMpxAppPaths(),
		getConfigPath: (config) => config.appPath,
		createDiscoveredConfig: (candidate) => {
			const config = { appPath: candidate };
			require_preset_resolution.assignDefined(config, {
				sourceRoot,
				sourceFileName,
				outputName,
				files,
				include,
				exclude
			});
			if (rules !== void 0) config.rules = rules;
			else if (sourceFileName === void 0) config.rules = [{
				from: { ref: "app.css" },
				to: require_preset_resolution.createStyleRuleTargets(files, include, exclude)
			}];
			return config;
		}
	}).flatMap(resolveMpxSubPackages);
	const injectorOptions = { ...rest };
	if (resolvedSubPackages.length > 0) {
		injectorOptions.subpackageStyleScopes = resolvedSubPackages;
		injectorOptions.generateSubpackageStyle = require_preset_resolution.createSyncScopeGenerator(resolvedSubPackages);
		injectorOptions.loadSubpackageTargetStyle = require_preset_resolution.loadSubpackageTargetStyle;
	}
	return require_webpack.weappStyleInjectorWebpack(injectorOptions);
}
//#endregion
Object.defineProperty(exports, "StyleInjector", {
	enumerable: true,
	get: function() {
		return StyleInjector;
	}
});
Object.defineProperty(exports, "resolveDefaultMpxAppPaths", {
	enumerable: true,
	get: function() {
		return resolveDefaultMpxAppPaths;
	}
});
Object.defineProperty(exports, "resolveMpxSubPackages", {
	enumerable: true,
	get: function() {
		return resolveMpxSubPackages;
	}
});
