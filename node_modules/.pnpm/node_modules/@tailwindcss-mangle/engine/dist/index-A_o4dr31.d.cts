import { n as TailwindStyleSource, t as TailwindStyleCandidateOptions } from "./style-candidates-BAuSaWWz.cjs";
import { SourceEntry } from "@tailwindcss/oxide";
import { Node, Rule } from "postcss";
import { Config } from "tailwindcss";
//#region src/types.d.ts
type TailwindcssClassCacheEntry = Rule | {
  layer: string;
  options: Record<string, any>;
  sort: Record<string, any>;
};
type TailwindcssClassCache = Map<string, TailwindcssClassCacheEntry[]>;
interface TailwindcssRuntimeContext {
  applyClassCache: Map<any, any>;
  candidateRuleCache: Map<string, Set<[{
    arbitrary: any;
    index: any;
    layer: string;
    options: any[];
    parallelIndex: any;
    parentLayer: string;
    variants: any;
  }, Node]>>;
  candidateRuleMap: Map<string | string, [object, Node][]>;
  changedContent: any[];
  classCache: TailwindcssClassCache;
  disposables: any[];
  getClassList: (...args: any[]) => any;
  getClassOrder: (...args: any[]) => any;
  getVariants: (...args: any[]) => any;
  markInvalidUtilityCandidate: (...args: any[]) => any;
  markInvalidUtilityNode: (...args: any[]) => any;
  notClassCache: Set<string>;
  offsets: {
    layerPositions: object;
    offsets: object;
    reservedVariantBits: any;
    variantOffsets: Map<string, any>;
  };
  postCssNodeCache: Map<object, [Node]>;
  ruleCache: Set<[object, Node]>;
  stylesheetCache: Record<string, Set<any>>;
  tailwindConfig: Config;
  userConfigPath: string | null;
  variantMap: Map<string, [[object, (...args: any[]) => unknown]]>;
  variantOptions: Map<string, object>;
}
interface ExtractResult {
  classList: string[];
  classSet: Set<string>;
  filename?: string;
}
interface TailwindTokenLocation {
  rawCandidate: string;
  file: string;
  relativeFile: string;
  extension: string;
  start: number;
  end: number;
  length: number;
  line: number;
  column: number;
  lineText: string;
}
type TailwindTokenFileKey = 'relative' | 'absolute';
interface TailwindTokenReport {
  entries: TailwindTokenLocation[];
  filesScanned: number;
  sources: SourceEntry[];
  skippedFiles: {
    file: string;
    reason: string;
  }[];
}
type TailwindTokenByFileMap = Record<string, TailwindTokenLocation[]>;
//#endregion
//#region src/v3/style-generator.d.ts
type TailwindV3ConfigInput = Partial<Config> & {
  content?: unknown;
  corePlugins?: unknown;
};
interface TailwindV3StyleGenerateOptions extends TailwindStyleCandidateOptions {
  /**
   * Tailwind v3 package name or alias. Defaults to `tailwindcss`.
   */
  packageName?: string;
  /**
   * `createRequire` base used to resolve the Tailwind v3 package.
   */
  cwd?: string;
  /**
   * Inline Tailwind v3 config. `content` is injected from collected candidates.
   */
  config?: TailwindV3ConfigInput;
  /**
   * Generate all layers by default. Pass a subset to emit only selected layers.
   */
  layers?: TailwindV3StyleLayer[];
}
type TailwindV3StyleLayer = 'base' | 'components' | 'utilities' | 'variants';
interface TailwindV3RawStyleGenerateOptions extends TailwindStyleCandidateOptions {
  /**
   * Tailwind v3 package name or alias. Defaults to `tailwindcss`.
   */
  packageName?: string;
  /**
   * `createRequire` base used to resolve the Tailwind v3 package.
   */
  cwd?: string;
  /**
   * Tailwind v3 entry CSS. Defaults to `@tailwind utilities;`.
   */
  css?: string;
  /**
   * Inline Tailwind v3 config. Candidate content is injected automatically.
   */
  config?: TailwindV3ConfigInput;
  /**
   * Directly append generated utility rules when the CSS is exactly `@tailwind utilities;`.
   * This mirrors Tailwind v3's internal fast path and keeps output order aligned.
   */
  directUtilitiesOnly?: boolean | 'auto';
}
interface TailwindV3RawStyleGenerateResult {
  version: 3;
  css: string;
  tokens: Set<string>;
  classSet: Set<string>;
  context: TailwindcssRuntimeContext;
  dependencies: string[];
  sources: TailwindStyleSource[];
  config: Config;
}
interface TailwindV3StyleGenerateResult {
  version: 3;
  css: string;
  tokens: Set<string>;
  classSet: Set<string>;
  sources: TailwindStyleSource[];
  config: Config;
}
declare function generateTailwindV3Style(options?: TailwindV3StyleGenerateOptions): Promise<TailwindV3StyleGenerateResult>;
declare function generateTailwindV3RawStyle(options?: TailwindV3RawStyleGenerateOptions): Promise<TailwindV3RawStyleGenerateResult>;
//#endregion
export { TailwindV3StyleGenerateResult as a, generateTailwindV3Style as c, TailwindTokenFileKey as d, TailwindTokenLocation as f, TailwindcssRuntimeContext as h, TailwindV3StyleGenerateOptions as i, ExtractResult as l, TailwindcssClassCache as m, TailwindV3RawStyleGenerateOptions as n, TailwindV3StyleLayer as o, TailwindTokenReport as p, TailwindV3RawStyleGenerateResult as r, generateTailwindV3RawStyle as s, TailwindV3ConfigInput as t, TailwindTokenByFileMap as u };